language: ruby
rvm:
  - 1.9.3
  - 2.0.0
  - 2.1.1
  - ruby-head
before_install:
  - sudo apt-get install -y libarchive-dev
  - export NOKOGIRI_USE_SYSTEM_LIBRARIES=1
matrix:
  allow_failures:
    - rvm: ruby-head
